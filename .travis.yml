language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-libra' ]; then cd .. && eval "mv $currentfolder generator-libra" && cd generator-libra; fi

